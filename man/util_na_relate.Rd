% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_na_relate.R
\name{util_na_relate}
\alias{util_na_relate}
\title{Test how missingness in each variable relates to every other variable}
\usage{
util_na_relate(eval_data, na_values = NULL, alpha = 0.05)
}
\arguments{
\item{eval_data}{An \code{eval_data} object}

\item{na_values}{A character vector of values that should be treated as
missing in addition to \code{NA}}

\item{alpha}{A significance threshold applied to BH-adjusted p-values when
classifying pairs in \code{na_relate_summary}. Defaults to \code{0.05}.}
}
\value{
A \code{list} (one per synthetic data replicate) with two tibbles,
restricted to the variables common to the confidential and synthetic data:
\itemize{
\item \code{na_relate}: one row per variable with a missing value (\code{na_var}) by
related variable (\code{related_var}), with diagnostics about the bivariate
relationship between the missingness and the related variable.
\itemize{
\item \code{method}: \code{"t_test"} when \code{related_var} is numeric, \code{"fisher"} when
categorical.
\item \code{effect_size_confidential}, \code{effect_size_synthetic}: Cohen's d for
\code{"t_test"} and the log odds ratio for \code{"fisher"} (\code{NA} when the table
is larger than 2x2 or the odds ratio is not finite). A positive value
means \code{related_var} is larger, or its second level is more common,
when \code{na_var} is missing.
\item \code{p_value_adj_confidential}, \code{p_value_adj_synthetic}: BH-adjusted
p-values. The false discovery rate is only controlled within a single
call, so adjusted p-values from separate calls (e.g. after subsetting
\code{na_var}s) are not comparable.
\item \code{effect_size_holdout}, \code{p_value_adj_holdout}: the same quantities for
the holdout data when supplied, and \code{NA} for pairs whose variables are
absent from it.
}
\item \code{na_relate_summary}: one row per \code{na_var} plus an overall row.
\itemize{
\item counts of pairs by where the relationship is significant: \code{preserved}
(confidential and synthetic), \code{lost} (confidential only), \code{spurious}
(synthetic only), and \code{consistent_null} (neither).
\item \code{effect_size_mae}: covers only pairs significant in the confidential
data that have an effect size in both sources, so it mixes Cohen's d
and log odds ratios and is not on a single scale.
\item \code{n_effect_size}: the number of relationships included in
\code{effect_size_mae}.
\item \code{concordance_rate}: \code{preserved / (preserved + lost)}.
}
}
}
\description{
Test how missingness in each variable relates to every other variable
}
\examples{
ed <- eval_data(conf_data = acs_conf, synth_data = acs_lr_synths[[1]])

util_na_relate(ed)

}
\seealso{
Other utility metrics:
\code{\link[=util_corr_fit]{util_corr_fit()}},
\code{\link[=util_moments]{util_moments()}},
\code{\link[=util_na_amount]{util_na_amount()}},
\code{\link[=util_na_cluster]{util_na_cluster()}},
\code{\link[=util_percentiles]{util_percentiles()}},
\code{\link[=util_totals]{util_totals()}}
}
\concept{utility metrics}
