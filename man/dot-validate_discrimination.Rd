% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrimination.R
\name{.validate_discrimination}
\alias{.validate_discrimination}
\title{Validate a discrimination object before an \verb{add_*()} step}
\usage{
.validate_discrimination(x, requires = NULL)
}
\arguments{
\item{x}{The object passed as \code{discrimination} to an \verb{add_*()} function.}

\item{requires}{A named character vector describing the elements this step
depends on. Each name is an element that must be non-\code{NULL} (for example
\code{"propensities"}); each value is the function that fills it (for example
\code{"add_propensities()"}), which is only used in the error message. \code{NULL}
means the step needs nothing beyond a valid \code{discrimination} object.}
}
\value{
\code{x}, invisibly, so the call can sit at the top of a function
without affecting its return value.
}
\description{
The discriminant-based metrics are built up in steps: \code{discrimination()}
creates the object, \code{add_propensities()} fills the \code{propensities} element,
and \code{add_pmse()}, \code{add_specks()}, and \code{add_discriminator_auc()} each read
that element to fill their own. \code{add_pmse_ratio()} in turn reads the \code{pmse}
element. Every element starts as \code{NULL}, so calling a step before the one it
depends on used to fail deep inside dplyr with a message about a missing
column. This helper runs first in each \verb{add_*()} function and turns that
into an error naming the step the user needs to run.
}
