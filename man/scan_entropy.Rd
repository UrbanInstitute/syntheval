% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_entropy.R
\name{scan_entropy}
\alias{scan_entropy}
\title{Quantify uncertainty about a target variable within equivalence classes}
\usage{
scan_entropy(attribute_scan, summarize = TRUE)
}
\arguments{
\item{attribute_scan}{An \code{attribute_scan} object.}

\item{summarize}{A logical. If \code{TRUE} (the default), collapses equivalence
classes down to the worst-case (minimum) entropy per \code{source} and
\code{target_var}, since lower entropy indicates a more predictable (riskier)
equivalence class. If \code{FALSE}, returns entropy for every equivalence class.}
}
\value{
If \code{summarize = TRUE}, a tibble with columns \code{source}
(\code{"confidential"}, \code{"synthetic"}, or \code{"holdout"} when holdout data is
available), \code{target_var}, \code{key_id}, the quasi-identifying key columns,
\code{class_n}, \code{entropy}, and \code{max_entropy}, filtered to the equivalence class
with the minimum \code{entropy} for each \code{source}/\code{target_var}. If
\code{summarize = FALSE}, the same columns for every equivalence class: \code{source},
\code{target_var}, \code{key_id}, the quasi-identifying key columns, \code{class_n} (the
number of records in that equivalence class), \code{entropy} (the base-2 Shannon
entropy of the conditional target distribution within that equivalence
class), and \code{max_entropy} (the maximum possible entropy for that target
variable's number of levels, for comparison across target variables).
}
\description{
Quantify uncertainty about a target variable within equivalence classes
}
