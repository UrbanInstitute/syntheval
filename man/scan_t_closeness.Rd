% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_t_closeness.R
\name{scan_t_closeness}
\alias{scan_t_closeness}
\title{Measure how closely each equivalence class target distribution matches the
overall target distribution}
\usage{
scan_t_closeness(
  attribute_scan,
  metric = c("linf", "l1", "l2"),
  summarize = TRUE
)
}
\arguments{
\item{attribute_scan}{An \code{attribute_scan} object.}

\item{metric}{A string describing the distance metric between the equivalence
class and overall target distributions. One of \code{"linf"} (L-infinity distance),
\code{"l1"} (L1 distance), or \code{"l2"} (L2 distance), defaults to \code{"linf"}.}

\item{summarize}{A logical. If \code{TRUE} (the default), collapses equivalence
classes down to the worst-case (maximum) t-closeness per \code{source} and
\code{target_var}, since higher t-closeness indicates greater divergence from the
overall target distribution. If \code{FALSE}, returns t-closeness for every
equivalence class.}
}
\value{
If \code{summarize = TRUE}, a tibble with columns \code{source}
(\code{"confidential"}, \code{"synthetic"}, or \code{"holdout"} when holdout data is
available), \code{target_var}, \code{key_id}, the quasi-identifying key columns,
\code{class_n}, and \code{t_closeness}, filtered to the equivalence class with the
maximum \code{t_closeness} for each \code{source}/\code{target_var}. If \code{summarize = FALSE},
the same columns for every equivalence class: \code{source}, \code{target_var},
\code{key_id}, the quasi-identifying key columns, \code{class_n} (the number of
records in that equivalence class), and \code{t_closeness} (the distance between
the equivalence class and overall target distributions).
}
\description{
Measure how closely each equivalence class target distribution matches the
overall target distribution
}
