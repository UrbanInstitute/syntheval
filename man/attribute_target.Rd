% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attribute_target.R
\name{attribute_target}
\alias{attribute_target}
\title{Create an attribute_target object for a model-based known-target attack}
\usage{
attribute_target(
  eval_data,
  workflow,
  prop = 3/4,
  grid = NULL,
  v = 10,
  fit_holdout = TRUE,
  save_fit = TRUE
)
}
\arguments{
\item{eval_data}{An \code{eval_data} object.}

\item{workflow}{A \code{workflows::workflow} with a model added via
\code{workflows::add_model()} and a preprocessor added via
\code{workflows::add_recipe()} or \code{workflows::add_formula()}. The model spec's
mode must be \code{"classification"} or \code{"regression"}. Build the recipe/formula
against \code{common_data(eval_data)} rather than the confidential, synthetic, or
holdout data directly, so the workflow only uses variables (and factor
levels) present across all of them.}

\item{prop}{The proportion of each fitting data set (synthetic and, if
available, holdout) retained for training in an internal train/test split.
Defaults to 3/4.}

\item{grid}{An optional tibble of hyperparameters for tuning. When supplied,
the workflow is tuned via cross-validation on the training split before the
final fit.}

\item{v}{The number of cross-validation folds used when \code{grid} is supplied.
Defaults to 10.}

\item{fit_holdout}{A logical for if the workflow should also be fit on
\code{eval_data$holdout_data} when it is supplied. Defaults to \code{TRUE}.}

\item{save_fit}{A logical for if the fitted model objects should be kept in
the returned object. Defaults to \code{TRUE}.}
}
\value{
An \code{attribute_target} object. If \code{eval_data$holdout_data} is
supplied and \code{fit_holdout} is \code{TRUE}, the object also includes a \code{holdout}
element with the same structure as \code{synthetic}.
}
\description{
Create an attribute_target object for a model-based known-target attack
}
