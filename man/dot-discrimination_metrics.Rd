% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrimination.R
\name{.discrimination_metrics}
\alias{.discrimination_metrics}
\title{Collect the computed discriminant metrics into one tibble}
\usage{
.discrimination_metrics(x)
}
\arguments{
\item{x}{A \code{discrimination} object.}
}
\value{
A \code{tibble} with columns \code{.metric}, \code{.label}, \code{.sample}, and \code{.value},
one row per computed metric and sample split, in display order. \code{.label} is
the metric name as shown by \code{print()}. Zero rows if nothing has been
computed.
}
\description{
The metrics live in three elements with different shapes: \code{pmse} and \code{specks}
use a \code{.source} column for the training/testing split, \code{discriminator_auc}
uses \code{.sample}, and \code{add_pmse_ratio()} adds \code{.null_pmse} and \code{.pmse_ratio}
as extra columns on the \code{pmse} element rather than an element of its own. This
helper normalises all of them to \code{.metric}, \code{.sample}, \code{.value} so that
\code{print.discrimination()} can lay them out in a single table.
}
